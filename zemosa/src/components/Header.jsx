const Header = () => {
    const headerStyles = {
        backgroundColor: "#ddd",
        color: "red",
        fontSize: "1.6rem",
        textAlign: "center",
    };
    return <div style={headerStyles}>Header</div>;
};

export default Header;
