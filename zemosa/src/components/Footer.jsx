const Footer = () => {
    const footerStyles = {
        backgroundColor: "#ddd",
        color: "red",
        fontSize: "1.6rem",
        textAlign: "center",
    };
    return <div style={footerStyles}>Footer</div>;
};

export default Footer;
