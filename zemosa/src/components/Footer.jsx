const Footer = () => {
    const footerStyles = {
        backgroundColor: "#ddd",
        color: "red",
        fontSize: "1.6rem",
        textAlign: "center",
    };
    return (
        <div style={footerStyles} className="footer">
            Footer
        </div>
    );
};

export default Footer;
